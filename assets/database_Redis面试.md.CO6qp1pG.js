import{_ as i,o as a,c as n,a2 as t}from"./chunks/framework.BZO4qERu.js";const l="/imgs/Redis%E7%BD%91%E7%BB%9C%E6%A8%A1%E5%9E%8B.png",c=JSON.parse('{"title":"Redis分布式锁","description":"","frontmatter":{},"headers":[],"relativePath":"database/Redis面试.md","filePath":"database/Redis面试.md","lastUpdated":1780998639000}'),e={name:"database/Redis面试.md"};function o(r,s,h,p,k,d){return a(),n("div",null,[...s[0]||(s[0]=[t(`<h1 id="redis分布式锁" tabindex="-1">Redis分布式锁 <a class="header-anchor" href="#redis分布式锁" aria-label="Permalink to &quot;Redis分布式锁&quot;">​</a></h1><h2 id="使用场景" tabindex="-1">使用场景： <a class="header-anchor" href="#使用场景" aria-label="Permalink to &quot;使用场景：&quot;">​</a></h2><p><strong>在课程下单以及领优惠卷中进行锁的实现，防止高并发下超卖现象的出现</strong></p><p>例如： 两个线程都在请求下单或者领劵的业务，线程一查询库存，发现为一，同时线程二也查到库存为一，此时线程一和二都不会报错，都会进行扣减库存的操作，这样库存数量就变为了负一，出现了超卖！</p><h2 id="尝试使用多线程中的synchronized加锁" tabindex="-1">尝试使用多线程中的synchronized加锁 <a class="header-anchor" href="#尝试使用多线程中的synchronized加锁" aria-label="Permalink to &quot;尝试使用多线程中的synchronized加锁&quot;">​</a></h2><h3 id="在单体项目中-使用-synchronized-没有问题" tabindex="-1">在单体项目中，使用 <code>synchronized</code> 没有问题： <a class="header-anchor" href="#在单体项目中-使用-synchronized-没有问题" aria-label="Permalink to &quot;在单体项目中，使用 \`synchronized\` 没有问题：&quot;">​</a></h3><p>线程1先获取锁，其他线程尝试获取锁失败后会被阻塞等待；等线程1完成查库存、扣减库存等业务逻辑并释放锁后，其他线程才能获取锁继续执行。</p><h3 id="在微服务架构下" tabindex="-1">在微服务架构下 <a class="header-anchor" href="#在微服务架构下" aria-label="Permalink to &quot;在微服务架构下&quot;">​</a></h3><p>同一个服务通常有多个实例，而 <code>synchronized</code> 是 JVM 级别的锁，只能保证<strong>单个实例内</strong>的线程互斥，无法约束其他实例。因此需要引入外部锁（例如<strong>分布式锁</strong>）来解决跨实例的并发问题。</p><h2 id="采用redis实现分布式锁" tabindex="-1">采用Redis实现分布式锁 <a class="header-anchor" href="#采用redis实现分布式锁" aria-label="Permalink to &quot;采用Redis实现分布式锁&quot;">​</a></h2><p>Redis实现分布式锁主要利用Redis的setnx命令。setnx是SET if not exists(如果不存在，则 SET)的简写。</p><ul><li>获取锁:</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#添加锁，NX是互斥、EX是设置超时时间</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SET</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lock</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> value</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> NX</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> EX</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 例如</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SET</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lock:order:123</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> unique_id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> NX</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> EX</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 里：</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">键：lock:order:123</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">值：unique_id</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">参数：NX（互斥）、EX</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 10（10秒过期）</span></span></code></pre></div><ul><li>释放锁：</li></ul><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">#安全释放锁，先判断是不是自己的锁再进行删除</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">#删除命令是：DEL key</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redis.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">KEYS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ARGV</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] then</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redis.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;del&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">KEYS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">end</span></span></code></pre></div><h3 id="命令分析" tabindex="-1">命令分析 <a class="header-anchor" href="#命令分析" aria-label="Permalink to &quot;命令分析&quot;">​</a></h3><p><strong>1. lock 和 value 分别是什么？</strong></p><ul><li><strong><code>lock</code></strong>：是的，这是你给锁起的<strong>名字</strong>（键），代表你要保护的资源，比如 <code>lock:order:123</code>。</li><li><strong><code>value</code></strong>：<strong>它不是“锁的具体值”，而是一个“令牌”，用来标识锁的持有者</strong>。为了防止误删别人的锁，通常设为一个唯一的客户端ID（如 UUID）。释放时，必须验证是自己的令牌才能删除。</li></ul><p><strong>2. 这是 <code>SETNX</code> 命令吗？</strong><strong>不是</strong>。虽然它们目的相似，但这是功能更强的 <code>SET</code> 命令的扩展用法。 这条命令的核心是 <code>SET key value [NX | XX] [EX seconds]</code> 这种组合形式，它的优势在于<strong>原子性</strong>。</p><p><strong>3. <code>SET ... NX EX</code> 与 <code>SETNX</code> 的核心区别</strong></p><ul><li><p><strong><code>SETNX</code> (SET if Not eXists)</strong> 一个过时的老命令。单纯执行 <code>SETNX lock value</code>，<strong>无法同时设置超时时间</strong>。如果先 <code>SETNX</code> 成功，再单独用 <code>EXPIRE</code> 设置过期时间，若两步之间客户端崩溃或网络中断，就会产生一个永远不会释放的“死锁”。因此，<strong>现在不推荐单独使用它来实现锁</strong>。</p></li><li><p><strong><code>SET ... NX EX</code> (现代原子命令)</strong><strong>一条命令，原子地完成了“加锁”和“设置超时”两个动作。</strong> <code>NX</code> 代表互斥（不存在才写入），<code>EX</code> 代表过期时间（秒）。这就彻底解决了死锁风险。<strong>Redis 官方和所有主流分布式锁实现现在都用这个。</strong></p></li></ul><p><strong>修正后的命令描述：</strong></p><ul><li><p><strong>获取锁</strong>：<code>SET lock_key client_unique_id NX EX 10</code></p><ul><li><code>lock_key</code>：锁的名字。</li><li><code>client_unique_id</code>：锁的持有者令牌（唯一标识），用于安全释放。</li><li><code>NX</code>：仅当键不存在时才能设置成功，实现互斥。</li><li><code>EX 10</code>：设置10秒后自动过期，防止死锁。</li></ul></li><li><p><strong>安全释放锁</strong>（不能用单纯的 <code>DEL</code>）： 你需要先检查 <code>lock_key</code> 的值是否等于自己的 <code>client_unique_id</code>，相等才删除。这需要借助 Lua 脚本保证“判断”和“删除”两步操作的原子性。</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redis.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;get&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, KEYS[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ARGV[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redis.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;del&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, KEYS[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div></li></ul><h2 id="加锁后流程" tabindex="-1">加锁后流程 <a class="header-anchor" href="#加锁后流程" aria-label="Permalink to &quot;加锁后流程&quot;">​</a></h2><p>加锁后流程与Synchronized是一样的，当有线程获取到锁之后，便向redis中存储锁的信息，此时其他线程便会获取锁失败进入阻塞状态，等持有锁的线程执行完业务逻辑并释放锁之后，再进行获取锁然后执行自己的业务逻辑，其他没锁的线程继续阻塞等待。</p><h2 id="关于锁的范围" tabindex="-1">关于锁的范围 <a class="header-anchor" href="#关于锁的范围" aria-label="Permalink to &quot;关于锁的范围&quot;">​</a></h2><p>锁本身不依赖于接口或者表，它锁的内容依据其实是位于加锁与释放锁重中间部分的代码内容，同时，锁的范围是结合两点来判断的，一是锁名，而是在代码逻辑上，同一锁名的加锁与释放锁中间的范围。</p><p><strong>锁的本质，就是一段由你自定义名字守护的、不可被并发执行的代码块。</strong></p><p>再帮你精简提炼一下，核心就是这两点：</p><ul><li><strong>锁的内容：</strong> 就是“获取锁”和“释放锁”之间的那段代码逻辑。这段代码要做什么（查库存、扣库存、发消息），锁完全不知情，它只负责保证这段逻辑同一时刻只被一个线程执行。</li><li><strong>锁的范围：</strong> 确实是由<strong>锁名</strong>和<strong>代码块边界</strong>两者共同决定的。 <ul><li><strong>锁名</strong>，决定了哪些线程会竞争同一把锁。</li><li><strong>代码块边界</strong>（即 <code>lock</code> 和 <code>unlock</code> 的位置），决定了被保护的逻辑范围有多大。</li></ul></li></ul><hr><h3 id="比喻-带编号的-安检门" tabindex="-1">比喻：带编号的“安检门” <a class="header-anchor" href="#比喻-带编号的-安检门" aria-label="Permalink to &quot;比喻：带编号的“安检门”&quot;">​</a></h3><p>可以把分布式锁系统想象成一排有无限多个、可以随时凭空出现的<strong>安检门</strong>。</p><ul><li><strong>锁名</strong>：就是贴在门上的<strong>编号</strong>（如 <code>order:123</code>）。</li><li><strong><code>lock()</code> 和 <code>unlock()</code></strong>：就是<strong>进门</strong>和<strong>出门</strong>的动作。</li><li><strong>被锁住的代码</strong>：就是<strong>在门里面需要安检处理的业务</strong>。</li></ul><p>当一个线程拿着编号 <code>order:123</code> 的锁来“进门”时：</p><ol><li>它会去找门上贴有 <code>order:123</code> 的门。</li><li>如果门里没人，它就进去，关上门开始处理业务。</li><li>此时，其他任何拿着同样编号 <code>order:123</code> 的线程，都会被挡在这扇门外等待，<strong>无论它们是要来查询还是修改</strong>。</li><li>当一个拿着编号 <code>order:456</code> 的线程到来时，它会去找 <code>order:456</code> 的门。这扇门的状态完全不受 <code>order:123</code> 门的影响，可以自由出入。</li></ol><p>所以，你之前提到的“接口锁”和“表锁”，其实只是我们在选择“锁名”和“加锁粒度”时的两种实践方式：</p><ul><li><strong>锁名用接口名（粗粒度）</strong>：相当于整个业务大厅只有一扇门，叫 <code>deductStock</code>。所有进大厅办事的人，无论办什么事，都只能排队一个个进。这很安全，但效率很低。</li><li><strong>锁名用数据ID（细粒度）</strong>：相当于为每个业务编号（如商品123）开了一扇独立的门。办理商品123业务的人只在自己的门前排队，办理商品456的人完全不受影响，可以同时进行。</li></ul><p>这也再次印证了你总结的规律：<strong>锁名和代码块共同定义范围</strong>。范围越大（锁名粗、代码块大），并发性能越差，但业务逻辑越简单；范围越小（锁名细、代码块小），性能越好。</p><h2 id="关于读锁和写锁-基于redission实现" tabindex="-1">关于读锁和写锁（基于Redission实现） <a class="header-anchor" href="#关于读锁和写锁-基于redission实现" aria-label="Permalink to &quot;关于读锁和写锁（基于Redission实现）&quot;">​</a></h2><h3 id="_1-以来门禁比喻" tabindex="-1">1.以来门禁比喻 <a class="header-anchor" href="#_1-以来门禁比喻" aria-label="Permalink to &quot;1.以来门禁比喻&quot;">​</a></h3><p>想象一个特殊的大门，它有一个智能门禁系统。这个门有三种状态，对应不同的钥匙权限：</p><ul><li><strong>无锁状态</strong>：门开着，谁都可以进。</li><li><strong>写锁状态</strong>：门被一个人用<strong>写卡</strong>进去后反锁了。此时，其他任何人，无论拿着<strong>读卡</strong>还是<strong>写卡</strong>，都必须在门外排队等。这是你理解的传统的互斥锁。</li><li><strong>读锁状态</strong>：门里有几个读者，他们用<strong>读卡</strong>进去的。但这扇门的神奇之处在于：<strong>它的门禁规则变了</strong>。它现在允许其他持有<strong>读卡</strong>的人也能直接进入，但他们不能修改屋里的任何东西。只有持有<strong>写卡</strong>的人会被挡在门外。</li><li>使用读锁的可以并发读，不能写，等所有持有读锁的线程都结束了，把锁释放了，才能进行获取写锁，执行后续业务。</li><li>使用写锁的，读和写都不能，只有等写锁被释放，其他线程才能操作</li></ul><p><strong>总结：</strong> “读锁”指<strong>锁的一种工作模式</strong>。当线程请求“读锁”时，它不是在请求一个排他的权限，而是在说：“只要当前没有人在<strong>写</strong>，就请放我进去，我只是个读者。”后续即便是在<strong>未释放读锁的情况下</strong>，只要线程请求的是读锁，就能正常获取到锁，继续执行。请求写锁的线程会被阻塞</p><p><strong>疑惑解答：</strong></p><blockquote><p>如果我在加锁的时候选择的是读锁，即便锁仍被其他进程占用，但是若该线程只需要查询操作的话就能正常查询吗？</p></blockquote><p><strong>是的，完全正确。</strong> 更准确地说：只要这个“占用”是<strong>读锁模式</strong>的占用，那么你这个新的读请求，就会被<strong>授予一把新的、共享的读锁</strong>，然后你就能正常执行查询业务逻辑了。你拿到了锁，只是这把锁是共享性质的。</p><blockquote><p>不能确定哪个线程执行的是哪个操作啊？这样怎么确定是读是写操作呢？</p></blockquote><p><strong>这由代码逻辑自己决定。</strong> 不同类型的锁调用不同类型的方法获取，程序知道自己的职责。一个只做查询的方法，会主动去申请<code>readLock</code>；一个做修改的方法，会主动去申请<code>writeLock</code>。门禁系统（Redis/Redisson）只根据锁类型和当前状态来决定放不放行。</p><h3 id="_2-完整的实例如下" tabindex="-1">2. 完整的实例如下： <a class="header-anchor" href="#_2-完整的实例如下" aria-label="Permalink to &quot;2. 完整的实例如下：&quot;">​</a></h3><p>假设我们有一个共享资源<code>库存数据</code>，我们用一把名为<code>lock:stock:123</code>的读写锁来保护它。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ============ 这是一个查询库存的方法 ============</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getStock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String productId) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RReadWriteLock rwLock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redisson.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getReadWriteLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lock:stock:&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> productId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RLock readLock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rwLock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">readLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    readLock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. 请求一把“读卡”</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 2. 执行查询业务逻辑</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> stockMapper.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">selectStock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(productId); </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">finally</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        readLock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3. 归还“读卡”</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ============ 这是一个扣减库存的方法 ============</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> deductStock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String productId) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RReadWriteLock rwLock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redisson.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getReadWriteLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lock:stock:&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> productId);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RLock writeLock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rwLock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">writeLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    writeLock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. 请求一把“写卡”</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 2. 执行扣减业务逻辑</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> currentStock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> stockMapper.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">selectStock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(productId);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (currentStock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            stockMapper.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">updateStock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(productId, currentStock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">finally</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        writeLock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 3. 归还“写卡”</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>场景演绎：</strong></p><ol><li><p><strong>时刻T1</strong>：线程A执行<code>deductStock</code>，成功获取<strong>写锁</strong>。此时它独占代码块，执行查询和修改。</p><ul><li><strong>门禁状态</strong>：<code>lock:stock:123</code>处于“写模式”。</li><li><strong>后果</strong>：所有后续的<code>readLock.lock()</code>和<code>writeLock.lock()</code>请求都会被阻塞，直到A释放。</li></ul></li><li><p><strong>时刻T2</strong>：线程A执行完毕，释放写锁。</p><ul><li><strong>门禁状态</strong>：变为无锁状态。</li></ul></li><li><p><strong>时刻T3</strong>：线程B执行<code>getStock</code>，成功获取第一个<strong>读锁</strong>。</p><ul><li><strong>门禁状态</strong>：变为“读模式”。</li><li><strong>后果</strong>：持有“写卡”的请求会被阻塞。</li></ul></li><li><p><strong>时刻T4</strong>：线程C也执行<code>getStock</code>。它请求<strong>读锁</strong>时，门禁系统检查发现当前是“读模式”，<strong>没有人在写</strong>，于是<strong>立即同意，授予线程C第二个读锁</strong>。</p><ul><li><strong>结果</strong>：线程B和线程C<strong>同时</strong>在执行查询代码块，并发访问数据库。这就是读锁共享。</li></ul></li><li><p><strong>时刻T5</strong>：线程B和C都还没结束。线程D执行<code>deductStock</code>，请求<strong>写锁</strong>。</p><ul><li><strong>后果</strong>：门禁系统检查发现当前是“读模式”，有人在读，写锁必须等待。所以线程D被阻塞，直到所有读者（B和C）都释放读锁。</li></ul></li></ol><h3 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h3><p>读写锁只是增加了一个<strong>巧妙的规则</strong>：</p><ul><li><strong>锁的范围</strong>，依然是“锁名 + 代码块”。</li><li><strong>但获取锁的条件，不再是简单的“无锁”，而是“没有冲突类型的锁存在”。</strong><ul><li><strong>读锁</strong>与<strong>读锁</strong>之间，被认为是不冲突的，可以共享代码块。</li><li><strong>写锁</strong>与<strong>任何锁</strong>，都被认为是冲突的，必须独占代码块。</li></ul></li></ul><p>这样就解决了“在保证数据一致性的前提下，如何让大量查询操作不被不必要地串行化”的问题。提升性能</p><h2 id="关于锁的过期时间" tabindex="-1">关于锁的过期时间 <a class="header-anchor" href="#关于锁的过期时间" aria-label="Permalink to &quot;关于锁的过期时间&quot;">​</a></h2><p>如果不设定过期时间，若是在执行业务的中间部分，服务器宕机了，锁将永远不会释放。安全性不高</p><h2 id="锁的过期时间如何确定" tabindex="-1">锁的过期时间如何确定 <a class="header-anchor" href="#锁的过期时间如何确定" aria-label="Permalink to &quot;锁的过期时间如何确定&quot;">​</a></h2><ol><li><p>预估锁的时间，这个一看就不行，不能确定业务执行耗时到底是多久，加上服务器性能，访问并发数量，网络情况都会影响业务耗时</p></li><li><p>利用Redission实现分布式锁，实现<strong>锁的续期</strong></p><p>流程如下： 当线程获取到锁之后，会有一个单独的线程watchDog来监听锁的执行状态，如果锁在过了设定过期时间（假设是30s）的1/3还没有完成逻辑的执行（此时还有20s），便会给锁的过期时间进行续期，重置过期时间为初始值（20s =&gt; 30s），</p><p>此时其他线程会尝试获取锁，获取失败则采用重试机制进行循环获取，一定时间后还没有获取到便阻塞进程，提高性能</p><p><strong>注意：所有的Redis命令使用Lua脚本完成，保证执行的原子性</strong></p></li></ol><h2 id="关于watchdog-基于redission实现" tabindex="-1">关于WatchDog（基于Redission实现） <a class="header-anchor" href="#关于watchdog-基于redission实现" aria-label="Permalink to &quot;关于WatchDog（基于Redission实现）&quot;">​</a></h2><p>Watch Dog 的检测依据是锁的过期时间，但严格来说，它依据的是<strong>Redisson内部预定义的默认过期时间（30秒）</strong>，而不是你自己指定的。</p><p>关于你问的“如果不设置呢”，答案非常核心：</p><p><strong>1. 触发Watch Dog的唯一条件：你没设置过期时间</strong> 只有当你调用 <code>lock()</code> 这种<strong>不带 <code>leaseTime</code>（租约时间）参数</strong>的方法时，Redisson 才会启用 Watch Dog 自动续期。</p><ul><li><strong>正确做法</strong>：<code>lock.lock();</code> 或 <code>lock.lock(30, TimeUnit.SECONDS);</code>（指等待时间，不是锁过期时间）</li><li><strong>此时机制</strong>：Redisson 会先把锁的过期时间设为<strong>内置默认的30秒</strong>。然后每过1/3（10秒），Watch Dog检查线程是否还活着，如果是，就重置回30秒。</li></ul><p><strong>2. 如果你自己设了过期时间，Watch Dog会“罢工”</strong> 一旦你指定了锁的持有时间，比如 <code>lock.lock(8, TimeUnit.SECONDS)</code>，Redisson 会认为你很清楚自己的业务最多跑8秒。</p><ul><li><strong>此时机制</strong>：锁的过期时间就是你设的8秒。时间一到，Redisson<strong>强制解锁</strong>，绝不续期。</li><li><strong>风险</strong>：如果业务没跑完，锁就丢了，可能造成并发问题。</li></ul><p><strong>总结一下两种模式的对比：</strong></p><ul><li><p><strong>自动续期模式 <code>lock()</code></strong> 你不管时间，Redisson 默认30秒，由 Watch Dog 自动续。适合无法预估业务执行时长的场景。</p></li><li><p><strong>固定时长模式 <code>lock(leaseTime, unit)</code></strong> 你指定持有时间，到期自动释放，Watch Dog 不起作用。适合能精确控制或需要强制等待的场景（如接口限流）。</p></li></ul><p><strong>需要特别注意的一点</strong> 刚才提到的“检查线程是否活着”只是表象。如果<strong>服务实例突然宕机，Watch Dog 进程也会停止，锁在30秒后会自动释放，不会死锁。</strong></p><hr><p><strong>一个你可能关心的延伸风险</strong></p><p>如果你是<strong>主从集群</strong>，这种锁并非绝对安全。当客户端刚在主节点加完锁，数据还没来得及同步到从节点，主节点就宕机了，新选出的主节点可能又给另一个客户端加了锁。</p><p><strong>对于强一致性的场景，可以考虑 Redisson 的 <code>RedLock</code>（红锁）</strong>，它需要依次在多个独立的 Redis 节点上获取锁，容错性更高，但性能会下降。</p><h2 id="关于锁的重入-基于redission分布式锁" tabindex="-1">关于锁的重入（基于Redission分布式锁） <a class="header-anchor" href="#关于锁的重入-基于redission分布式锁" aria-label="Permalink to &quot;关于锁的重入（基于Redission分布式锁）&quot;">​</a></h2><p>基于Redis的分布式锁不具备锁的重入。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 锁重入的实例</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(){</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	RLock lock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redissonClient.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;heimalock&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 尝试获取锁</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> isEock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tryLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 执行业务</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">	add2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// 1.释放锁</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        lock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> add2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(){</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	RLock lock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redissonClient.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;heimalock&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 尝试获取锁</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> isLock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tryLock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     //执行业务</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// 释放锁</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	lock.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>依据线程id进行判断获取锁的是不是同一个线程，若是同一个线程便运行获取同一个锁，实现锁的重入。</p><h3 id="具体细节" tabindex="-1">具体细节 <a class="header-anchor" href="#具体细节" aria-label="Permalink to &quot;具体细节&quot;">​</a></h3><p>利用利用<strong>hash结构记录线程id和重入次数</strong></p><p>用锁名作为哈希中的key值，value中存储线程的唯一标识id，以及该线程重入次数</p><p>当执行一次unlock时，先去缓存中查询当前锁的信息，然后把重入次数减一，直到重入次数为0，说明所有业务已执行完毕，彻底释放锁，删除锁的信息。</p><h3 id="可避免死锁问题" tabindex="-1">可避免死锁问题 <a class="header-anchor" href="#可避免死锁问题" aria-label="Permalink to &quot;可避免死锁问题&quot;">​</a></h3><p><strong>避免死锁</strong>：可重入锁允许同一个线程多次获取同一个锁而不会导致死锁。如果锁不可重入，当同一个线程在持有锁的情况下再次请求该锁，就会被自己阻塞，造成死锁。</p><h2 id="主从集群情况下的redission分布式锁的一致性问题" tabindex="-1">主从集群情况下的Redission分布式锁的一致性问题 <a class="header-anchor" href="#主从集群情况下的redission分布式锁的一致性问题" aria-label="Permalink to &quot;主从集群情况下的Redission分布式锁的一致性问题&quot;">​</a></h2><p>这是单纯用单节点 Redis 的 <code>SET NX EX</code> 命令实现分布式锁，在主从架构下不安全的根本原因。</p><p>Redis 的主从同步默认是<strong>异步</strong>的，它优先保证<strong>可用性（AP）</strong>，而不是<strong>一致性（CP）</strong>。你遇到的情况，就是 CAP 理论中，选择了可用性而牺牲一致性的典型后果。</p><p>Redis一般是主从集群部署，主节点负责增删改，从节点负责查询，一般是在主节点改动数据后，再通过主节点把数据同步到从节点上。</p><p>但是当线程获取到锁并在主节点进行修改数据过程中，业务逻辑还没执行完，还未释放锁，主节点便宕机了，数据自然也没有同步过去，此时基于哨兵模式，会从后续的从节点重新选一个作为主节点，若又来了一个新的线程来请求这个新的主节点进行修改，会造成<strong>两个不同的客户端线程，先后持有了两把“逻辑上本应互斥”的锁</strong>。，且造成数据不一致的问题，出现脏数据。</p><ol><li><strong>初始状态</strong>：客户端 A 在主节点成功获取锁。</li><li><strong>故障发生</strong>：A 还没完成业务，<strong>锁的 key 还没同步到从节点</strong>，主节点就宕机了。</li><li><strong>故障转移</strong>：哨兵集群发现主节点宕机，将一个从节点提升为新主节点。<strong>此时，新主节点的内存里没有 A 的锁记录。</strong></li><li><strong>新的请求</strong>：客户端 B 向<strong>新主节点</strong>申请同一把锁，<strong>申请成功</strong>。它以为自己独占了锁。</li><li><strong>冲突产生</strong>：此时，客户端 A 以为自己还持有锁（其实它在旧主节点上），客户端 B 也以为自己持有锁（在新主节点上）。它们在逻辑上进入了互斥区，导致数据不一致。</li></ol><h2 id="可以用红锁redlock解决-不常用-麻烦" tabindex="-1">可以用红锁RedLock解决（不常用，麻烦） <a class="header-anchor" href="#可以用红锁redlock解决-不常用-麻烦" aria-label="Permalink to &quot;可以用红锁RedLock解决（不常用，麻烦）&quot;">​</a></h2><ul><li>RedLock(红锁)：不能只在一个redis实例上创建锁，应该是在多个redis实例上创建锁(n／2+1)奇数个，避免在一个redis实例上加锁。</li><li>Redis 的作者提出了 <strong>RedLock 算法</strong>。它的核心思想是<strong>不在一个中心节点上“赌博”，而是去多个独立的节点上“投票”</strong>。</li></ul><p>具体流程：</p><ol><li><strong>部署奇数个独立的节点</strong>：不采用主从结构，而是几个完全独立的 Redis 主节点，谁也不同步谁。</li><li><strong>依次获取锁</strong>：客户端需要依次向这 几个节点申请锁。每次申请都设置一个很短的超时时间（如几十毫秒）。</li><li><strong>多数成功才算数</strong>：只有当客户端在大多数节点（比如 5 个里拿到 3 个）上成功获取了锁，且总耗时小于锁的有效期时，才算最终获取锁成功。</li><li><strong>失败则全释放</strong>：如果拿到不够多数，它会向所有节点（包括之前成功的）发送释放锁的请求。</li></ol><p>这样，只要不超过半数的节点宕机，系统就不会出现两个客户端同时拿到锁的情况。你描述的单点故障导致锁丢失的问题就被解决了。</p><p><strong>不过要注意，红锁本身也有一些争议和局限性</strong></p><h3 id="主要争议" tabindex="-1">主要争议 <a class="header-anchor" href="#主要争议" aria-label="Permalink to &quot;主要争议&quot;">​</a></h3><p>RedLock 虽强，但并非无懈可击，主要争议点在于：</p><ul><li><strong>依赖时钟</strong>：算法假设所有节点的时钟误差很小。如果一个节点时钟突然跳变（如 NTP 校时），可能导致锁提前过期，破坏互斥。</li><li><strong>进程停顿风险</strong>：客户端拿到锁后，如果发生长时间的 GC 停顿或网络延迟，锁在 Redis 端自己过期了，程序却不知道，仍会继续写入，造成并发冲突。这个问题单机 Redis 锁也有，RedLock 并未解决，需要配合版本号等机制。</li><li><strong>复杂度高</strong>：维护多套独立 Redis 的成本和故障处理比主从集群复杂得多，有人认为不如直接上 ZooKeeper 这类基于一致性协议（ZAB）的方案。</li></ul><p>所以实践中，如果数据一致性容忍度极低，团队会选择 CP 组件（如 ZooKeeper、etcd）来实现锁；如果能接受极小概率的锁冲突，那么单实例 Redis 或主从架构加上合理的业务兜底（如数据库唯一约束）往往也够用了。</p><p><strong>ZooKeeper 和 etcd 实现分布式锁</strong>：</p><p>性能不如 Redis，运维更复杂。因此一般只在极端追求数据一致性、容忍不了任何锁丢失的场景下选用。</p><h2 id="redis是单线程-为什么还那么快" tabindex="-1">Redis是单线程，为什么还那么快 <a class="header-anchor" href="#redis是单线程-为什么还那么快" aria-label="Permalink to &quot;Redis是单线程，为什么还那么快&quot;">​</a></h2><h3 id="redis是纯内存操作-执行速度非常快" tabindex="-1">Redis是纯内存操作，执行速度非常快 <a class="header-anchor" href="#redis是纯内存操作-执行速度非常快" aria-label="Permalink to &quot;Redis是纯内存操作，执行速度非常快&quot;">​</a></h3><p>Redis所有数据都在内存中，读写不涉及磁盘IO（持久化是异步的）。内存的响应时间是<strong>纳秒级</strong>，而传统数据库的磁盘寻道是<strong>毫秒级</strong>。这意味着一条命令的执行本身极快，CPU轻易不会成为瓶颈，瓶颈更容易出现在网络IO上。</p><h3 id="采用单线程-避免不必要的上下文切换可竞争条件-多线程还要考虑线程安全问题" tabindex="-1">采用单线程，避免不必要的上下文切换可竞争条件，多线程还要考虑线程安全问题 <a class="header-anchor" href="#采用单线程-避免不必要的上下文切换可竞争条件-多线程还要考虑线程安全问题" aria-label="Permalink to &quot;采用单线程，避免不必要的上下文切换可竞争条件，多线程还要考虑线程安全问题&quot;">​</a></h3><p>这其实是选择了一种<strong>极简的并发模型</strong>。</p><ul><li><strong>避免了上下文切换开销</strong>：多线程模型里，CPU在线程间频繁“换人”会消耗资源。Redis单线程像一根直线，CPU一直高效执行同一任务。</li><li><strong>避免了同步原语的开销</strong>：多线程操作共享数据必须用锁或原子操作。这不仅有获取/释放锁的CPU开销，还会带来锁竞争导致的线程阻塞和编程复杂度。Redis单线程天然线程安全，无需任何锁机制，数据结构实现也能保持简洁高效。</li></ul><h3 id="上下文切换为什么消耗资源" tabindex="-1">. 上下文切换为什么消耗资源？ <a class="header-anchor" href="#上下文切换为什么消耗资源" aria-label="Permalink to &quot;. 上下文切换为什么消耗资源？&quot;">​</a></h3><p>想象你正在全神贯注地组装一个复杂的乐高模型（<strong>线程A</strong>），所有零件都摊在桌面上。这时电话响了，你必须去处理一件紧急事情（<strong>线程B</strong>）。</p><ul><li><strong>保存现场（保存上下文）</strong>：你得先记住现在拼到哪了，图纸翻到第几页，手里的零件是哪块，然后小心翼翼地把模型放到一边。对应到CPU，就是要把当前线程的寄存器状态、程序计数器（下一条指令地址）等信息保存到内存里。</li><li><strong>加载新现场（恢复上下文）</strong>：然后你拿起电话，翻开新的笔记本，回忆事情的前因后果，开始处理电话。CPU要把下一个线程的状态从内存加载到寄存器中。</li><li><strong>无价值的开销</strong>：这个“放下模型、拿起电话、回忆事情”的过程本身，<strong>不产生任何直接的生产力</strong>。你既没有继续拼乐高，也没有推进电话里的事情，只是在做切换动作。在计算机里，如果切换频繁，CPU大量的时间就花在这个“保存/恢复”的开销上，真正用于执行程序的时间就少了。</li><li><strong>缓存失效的代价</strong>：更糟糕的是，你刚才摆满桌子的乐高零件（对应CPU高速缓存中的数据），现在全要被收起来，给电话的事情腾出桌面。等你处理完电话，再回来继续拼模型时，又得重新把零件一一摆出来。这个“重新摆零件”的动作，在计算机里就是<strong>CPU高速缓存（L1/L2/L3）大量失效</strong>，数据得从慢得多的内存里重新读取，非常耽误时间。</li></ul><p><strong>而Redis单线程就像一根直线</strong>：它只有一个任务，没有谁打断它，不需要“保存/恢复现场”，所有数据都热乎乎地待在CPU缓存里，可以一直全速运转</p><p>简而言之：<strong>既然执行这么快，更没必要引入多线程的复杂度和额外开销。</strong> 瓶颈不在这里。</p><h3 id="什么是原子操作" tabindex="-1">什么是原子操作？ <a class="header-anchor" href="#什么是原子操作" aria-label="Permalink to &quot;什么是原子操作？&quot;">​</a></h3><p>“原子”在这里指<strong>不可分割的最小单位</strong>，就像物理里的原子（当时认为是不可分割的）。一个原子操作，要么<strong>完全执行完，要么根本不执行</strong>，不存在“执行到一半”的中间状态被别的线程看到。</p><p>为了让程序正确，我们常需要把一段代码变得“原子化”。主要有两种方式：</p><ul><li><strong>锁（Lock）</strong>：像厕所门上的锁，一个人进去后锁上门，别人在外面等。这保证了只有拿到锁的人能执行那段代码。</li><li><strong>原子操作（Atomic Operation）</strong>：这相当于不用锁，而是CPU提供了一些“组合指令”，它直接对你说：“你尽管做，我保证一步到位，不会被打断。”</li></ul><p><strong>为什么需要原子操作？</strong> 比如很常见的 <code>i = i + 1</code> 这条代码，看起来是一步，但在计算机底层其实是三步：</p><ol><li><strong>读取</strong>：把变量 <code>i</code> 当前的值（比如 0）从内存读到CPU寄存器。</li><li><strong>计算</strong>：在寄存器里给这个值加 1（得到 1）。</li><li><strong>写回</strong>：把新值（1）写回内存中的变量 <code>i</code>。</li></ol><p><strong>多线程下的问题场景：</strong></p><ul><li>线程A执行完第1步，拿到了 0，还没来得及加1，时间片到了，被切换出去。</li><li>线程B接手，完整地执行了读取、加1、写回三个步骤，把 <code>i</code> 从 0 变成了 1。</li><li>线程A再切换回来，但它手里的值还是刚才读到的 0，它接着完成加1，得到 1，然后写回内存。最终内存里的 <code>i</code> 还是 1。</li></ul><p>两个线程各加了1，本来 <code>i</code> 应该是 2，结果却是 1，这就产生了数据错误。</p><p>如果用<strong>锁</strong>，就是把 <code>i++</code> 整个包裹起来，同时只能有一个人做。而如果用<strong>原子操作</strong>，比如 CAS（Compare-And-Swap，比较并交换），CPU会通过硬件锁定内存总线或缓存行，确保在“读取-计算-写回”这个过程中，没有其他CPU能打扰这块数据，仿佛真的是“一步完成”。在Java里，<code>AtomicInteger</code> 类就利用了底层的CAS指令来实现 <code>incrementAndGet()</code> 方法，无需显式加 <code>synchronized</code>。</p><p><strong>回到Redis的情境</strong>：由于Redis的命令处理是单线程的，所以它执行 <code>INCR</code> 这类命令时，天然就是原子的——没有第二个线程会同时在操作同一个Key。它既不需要锁，也不需要复杂的原子指令，代码和数据模型都能保持简洁。</p><h3 id="使用i-o多路复用模型-非阻塞io" tabindex="-1">使用I/O多路复用模型，非阻塞IO <a class="header-anchor" href="#使用i-o多路复用模型-非阻塞io" aria-label="Permalink to &quot;使用I/O多路复用模型，非阻塞IO&quot;">​</a></h3><p>这是解决<strong>单线程如何承载万级并发连接</strong>的关键技术。</p><ul><li><strong>阻塞IO的“原始”问题</strong>：传统方案里，<code>read()</code>调用会阻塞线程，直到有数据到达。一个线程就只能处理一个连接，要处理多个，就得开多个线程。</li><li><strong>非阻塞IO配合“多路复用”</strong>：Redis把<strong>监听任务</strong>交给操作系统内核（通过<code>epoll</code>等），一个线程能同时监听成千上万个socket。只有当某个socket有数据到达时，内核才通知Redis处理，Redis再执行极快的命令。这就好比一个收银员同时盯几百个窗口，但他只服务有顾客结账的窗口，核心处理动作极快。 若是采用原本的阻塞I/Od的话，哪怕后续多个socket已经有消息了，但read当前请求的socket没有数据，就会一直阻塞等待，只有当前socket有数据，并且执行完当前的后，才会继续读取后面的数据。</li></ul><h3 id="_1-关于-read-的理解" tabindex="-1">1. 关于 <code>read()</code> 的理解 <a class="header-anchor" href="#_1-关于-read-的理解" aria-label="Permalink to &quot;1. 关于 \`read()\` 的理解&quot;">​</a></h3><p>你的理解完全正确。</p><p>这个 <code>read()</code> 操作，准确地说是用户空间程序（比如Redis）向操作系统内核发起的一个<strong>系统调用</strong>，目的是把<strong>内核缓冲区</strong>中已经接收到的网络数据，拷贝到<strong>用户空间</strong>的缓冲区里，供程序使用。</p><p><strong>阻塞I/O之所以慢，是因为它把两个等待合二为一了：</strong></p><ol><li><strong>等数据到达网卡</strong>：数据得先从网络上来。</li><li><strong>等内核拷贝数据</strong>：内核把数据准备好，拷贝给用户程序。</li></ol><p>在阻塞I/O模型下，只要第一步数据没到，线程就会完全卡死在 <code>read()</code> 上，什么也干不了，直到数据就绪并拷贝完成。</p><h3 id="_2-socket-是什么" tabindex="-1">2. <code>socket</code> 是什么？ <a class="header-anchor" href="#_2-socket-是什么" aria-label="Permalink to &quot;2. \`socket\` 是什么？&quot;">​</a></h3><p><code>socket</code>（套接字）你可以把它理解为：</p><blockquote><p><strong>网络通信中的一个端点，是应用程序连接网络世界的“插头”或“文件描述符”。</strong></p></blockquote><p>在Linux系统里，一切皆文件，网络连接也不例外。<code>socket</code> 就是一个特殊的文件，对它进行读写，就实现了网络通信。</p><ul><li><strong>一对 <code>socket</code> 构成一条连接</strong>：一条TCP连接，由客户端的一个 <code>socket</code> 和服务端的一个 <code>socket</code>，一对一地配对组成。</li><li><strong><code>socket</code> 是数据的出入口</strong>：服务端想给客户端发数据，就往这个 <code>socket</code> 里“写”；想收数据，就从这个 <code>socket</code> 里“读”。</li></ul><p>你最后的总结非常到位。我们把它放入一个具体的类比，你就会看得更清晰：</p><p><strong>阻塞I/O模型：一个收银员（线程）守一个窗口（socket），死等</strong></p><ul><li>Redis（收银员A）负责看管 <code>socket-1</code>。</li><li>它调用 <code>read(socket-1)</code>，就像死死地盯着1号收银窗口。</li><li><strong>关键点来了</strong>：即使 <code>socket-2</code> 和 <code>socket-3</code> 的顾客（数据）已经排起了长队，按铃按得震天响，收银员A也完全不理。因为经理（操作系统）规定他必须死等1号窗口。只有1号窗口的顾客来了，他服务完，才能去处理下一件事。</li><li>这就导致了你要处理上万个 <code>socket</code>（窗口），就得雇佣上万个收银员（线程），成本极高。</li></ul><p><strong>I/O多路复用模型：一个收银员（线程）盯所有窗口（socket），谁亮灯服务谁</strong></p><ul><li>现在大厅装了一套先进的叫号/亮灯系统（<code>epoll</code>）。</li><li>收银员A只需要对经理说：“我休息会儿，这有N个窗口，哪个灯亮了（有数据可读）就叫我。”</li><li>然后他可以悠闲地等着。一旦 <code>socket-2</code> 的灯亮了，经理叫他，他立刻跑去处理 <code>socket-2</code>。</li><li>处理完这一个，他又回去等，循环往复。</li></ul><p>所以，<strong>阻塞I/O是串行等待，多路复用是并行监听，串行处理</strong>。因为处理本身（内存操作）极快，所以它就能用一个线程，在成千上万个连接间快速切换、高效服务。</p><h2 id="总结-1" tabindex="-1">总结 <a class="header-anchor" href="#总结-1" aria-label="Permalink to &quot;总结&quot;">​</a></h2><p>因为是<strong>纯内存操作</strong>，所以命令执行极快，这让<strong>单线程</strong>处理成为可能，从而避免了<strong>上下文切换和锁竞争</strong>的额外开销；而解决单线程承载高并发的办法，就是利用<strong>非阻塞IO和多路复用</strong>，高效地管理海量连接。</p><h2 id="解释一下i-o多路复用模型" tabindex="-1">解释一下I/O多路复用模型? <a class="header-anchor" href="#解释一下i-o多路复用模型" aria-label="Permalink to &quot;解释一下I/O多路复用模型?&quot;">​</a></h2><h3 id="i-o多路复用" tabindex="-1">I/O多路复用 <a class="header-anchor" href="#i-o多路复用" aria-label="Permalink to &quot;I/O多路复用&quot;">​</a></h3><p>是指利用单个线程来同时监听多个Socket，并在某个Socket可读、可写时得到通知，从而避免无效的等待，充分利用CPU资源。目前的I/O多路复用都是采用的epoll模式实现，它会在通知用户进程Socket就绪的同时，把已就绪的Socket写入用户空间，不需要挨个遍历Socket来判断是否就绪，提升了性能</p><h3 id="redis网络模型" tabindex="-1">Redis网络模型 <a class="header-anchor" href="#redis网络模型" aria-label="Permalink to &quot;Redis网络模型&quot;">​</a></h3><p>就是使用I/O多路复用结合事件的处理器来应对多个Socket</p><ul><li><p>请求连接应答处理器</p></li><li><p>命令回复处理器，在Redis6.0之后，为了提升更好的性能，使用了多线程来处理回复事件</p></li><li><p>命令请求处理器，在Redis6.0之后，将命令的转换使用了多线程，增加命令转换速度，在命令执行的时候，依然是单线程</p></li></ul><p><img src="`+l+'" alt=""></p><p>基于 I/O 多路复用，可以把 Redis 的网络模型想象成一个高效的前台。Redis 6.0 后，它把“接待客人的琐事”和“真正的业务处理”拆开了。</p><p>核心流程由这几个<strong>事件处理器</strong>分工完成：</p><h3 id="_1-请求连接应答处理器" tabindex="-1">1. 请求连接应答处理器 <a class="header-anchor" href="#_1-请求连接应答处理器" aria-label="Permalink to &quot;1. 请求连接应答处理器&quot;">​</a></h3><p>负责处理新来的连接请求。</p><ul><li>当有客户端通过 <code>socket</code> 尝试连接 Redis 时，多路复用器（<code>epoll</code>）会检测到这个 <code>socket</code> 的<strong>连接事件</strong>。</li><li>这个处理器被调用，完成 TCP 三次握手，<strong>建立连接</strong>。随后将新的 <code>socket</code> 注册到多路复用器中，开始监听它的读事件。</li><li><strong>本质</strong>：就是“迎宾”，让新客户能进门，之后的服务交给后面的处理器。</li></ul><h3 id="_2-命令请求处理器" tabindex="-1">2. 命令请求处理器 <a class="header-anchor" href="#_2-命令请求处理器" aria-label="Permalink to &quot;2. 命令请求处理器&quot;">​</a></h3><p>负责读取并解析客户端发来的命令。</p><ul><li>当已连接的 <code>socket</code> 有数据到达（读事件就绪），这个处理器被触发。</li><li><strong>Redis 6.0 前</strong>：全程由主线程干。它从 <code>socket</code> 里读数据，然后按 RESP 协议把字节流<strong>解析成具体的命令字符串</strong>（如 <code>GET name</code>）。</li><li><strong>Redis 6.0 后的变化（核心优化）</strong>：<strong>读取和解析命令</strong>的工作交给了<strong>多线程</strong>。 <ul><li>主线程把 <code>socket</code> 分配给工作线程，工作线程并行地读取数据并解析成命令对象。</li><li>解析完毕后，工作线程把命令对象交还给主线程。</li><li><strong>关键点</strong>：这只是把耗时的 I/O 读取和协议解析“外包”出去，真正<strong>执行命令</strong>（如 <code>GET</code> 操作内存数据）这一步，仍然由<strong>主线程单线程</strong>执行。</li></ul></li></ul><h3 id="_3-命令回复处理器" tabindex="-1">3. 命令回复处理器 <a class="header-anchor" href="#_3-命令回复处理器" aria-label="Permalink to &quot;3. 命令回复处理器&quot;">​</a></h3><p>负责把执行结果返回给客户端。</p><ul><li>主线程执行完命令后，会准备好要返回的数据（比如查询到的字符串）。</li><li><strong>Redis 6.0 前</strong>：主线程亲自把结果数据写回 <code>socket</code>。</li><li><strong>Redis 6.0 后的变化（核心优化）</strong>：<strong>将执行结果写回</strong>的工作也交给了<strong>多线程</strong>。 <ul><li>主线程只需把结果挂到对应客户端的输出缓冲区，然后通知工作线程去异步写回。</li><li>写回完成后，再通知多路复用器，这个 <code>socket</code> 又可以等待下一次读事件了。</li></ul></li></ul><h3 id="总结-为什么这么设计" tabindex="-1">总结：为什么这么设计？ <a class="header-anchor" href="#总结-为什么这么设计" aria-label="Permalink to &quot;总结：为什么这么设计？&quot;">​</a></h3><ul><li><strong>传统单线程瓶颈</strong>：并不是 CPU 执行命令慢，而是当连接数巨大时，大量的网络数据读取和写回操作会占满主线程的时间，让执行命令的“有效工作时间”变少。</li><li><strong>6.0 多线程本质</strong>：它把最耗时的 <strong>“网络 I/O”和“协议解析”</strong> 这两个脏活累活，从主线程剥离，交给线程池并行处理。</li><li><strong>不变的核心</strong>：<strong>修改内存数据的命令执行，依然是单线程的</strong>。这既保留了“无需担心锁”的简洁和原子性，又大幅提升了网络层面的吞吐能力。</li></ul>',168)])])}const E=i(e,[["render",o]]);export{c as __pageData,E as default};
